{
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "db:seed": "npm exec -- prisma generate && npm exec -- prisma db seed"
  },
  "prisma": { "seed": "tsx prisma/seed.ts" },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "prisma": "^6.10.1",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  }
}